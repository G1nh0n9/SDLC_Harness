# Agent Workflow Harness — Hermes 프로젝트 규칙

## 범위

이 저장소에서는 증거 중심 다중 에이전트 프로그래밍 워크플로만 설계·구현한다. 다른 과거 프로젝트의 코드·패치·시험 결과를 요구사항이나 근거로 끌어오지 않는다.

## 기준 문서

- `HANDOFF.md`: 현재 범위와 인계 상태
- `docs/agent-harness-governance.md`: 설계 기준안
- `evidence/workflow-improvement-ledger.json`: 외부 근거 원장

## 개발 원칙

- 구현 전에 시험 가능한 요구와 금지 동작을 명시한다.
- 상태 기계, 후보본 ID, 증거 무효화는 결정론적 코드로 구현한다.
- LLM의 지시 준수를 권한 경계로 간주하지 않는다.
- 역할별 작업 공간·도구·쓰기 경로를 실제로 분리한다.
- 검토 중인 후보본은 수정하지 않는다.
- 입력이 달라지면 새 후보본을 만들고 기존 증거를 자동으로 무효화한다.
- 시험·검토·패키징 결과는 동일한 후보본 ID와 해시에 묶는다.
- 수락 시험의 기대값을 구현자가 임의로 바꾸지 못하게 한다.
- 실패를 숨기거나 통과 조건을 완화하지 말고 실패한 단계로 되돌린다.
- 코드 작성만으로 완료라고 하지 않는다. 실제 시험과 결함 주입 검증 결과를 남긴다.

## 구현 방식

- Python 3.11 이상을 기준으로 한다.
- 단위 시험을 먼저 작성하고 RED-GREEN-REFACTOR 순서를 지킨다.
- JSON Schema로 모든 인계 문서와 증거 문서를 검사한다.
- 사건 원장은 append-only 방식으로 기록한다.
- 파일 쓰기와 빌드는 역할별 허용 경로 안에서만 수행한다.
- 최종 패키지는 같은 후보본에 속한 필수 증거가 모두 유효할 때만 만든다.

## 보고 기준

- 실제 실행한 명령, 종료 코드, 산출물 해시를 기록한다.
- 시험 통과를 전체 정확성의 증명으로 과장하지 않는다.
- 미검증 가정, 남은 위험, 범위 밖 항목을 명시한다.
